# `08` List Count

Haz una peticion `GET` a `/items`.

Inspecciona la respuesta e identifica el valor que te indica cuantos recursos hay disponibles actualmente en la coleccion.

Resultado esperado: `2`
