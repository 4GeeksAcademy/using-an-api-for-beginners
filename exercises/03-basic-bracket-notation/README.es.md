# `03` Basic Bracket Notation

Haz una peticion `GET /overview`.

Inspecciona la respuesta y encuentra el valor guardado en una clave que se accede mejor con bracket notation.

Resultado esperado: `"1.0"`
